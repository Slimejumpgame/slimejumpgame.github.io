"use strict";

// Coast follows the standard drop-in naming contract. Keeping this alias
// preserves the existing public visual handle while the registry owns loading,
// validation and fallback behavior for Coast and every future standard biome.
const COAST_ASSET_VISUALS = BIOME_PLATFORM_VISUALS.resolve("coast");
